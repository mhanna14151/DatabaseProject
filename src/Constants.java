import java.awt.*;

interface Constants
{
  final static Color LIGHT_GRAY = new Color(235, 235, 235);
  final static int EFFECTIVE_WIDTH = 1500;
  final static Dimension FULL_SCREEN = new Dimension(EFFECTIVE_WIDTH, 900);
}